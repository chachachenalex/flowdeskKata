{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "admin",
  "password": "password",
  "database": "marketDB",
  "synchronize": true,
  "logging": true,
  "entities": [ "src/entity/MarketData.ts" ],
  //"migrations": [ "src/migration/*.ts" ], do not need for instance ,maybe need to be removed once we record the migration
  "cli": {
    "entitiesDir": "src/entity",
    "migrationsDir": "src/migration"
  }
}
